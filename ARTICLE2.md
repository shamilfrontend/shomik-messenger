Отличная статья! У вас хорошая структура и много технических деталей. Я предлагаю улучшить стиль, добавить «соль» и немного переработать формат, чтобы статья стала более увлекательной и профессиональной. Вот улучшенная версия:

---

# Как я с нуля создал веб-мессенджер с аудио-звонками. Часть 1: Архитектура и первые вызовы

### Вместо вступления: а зачем вообще это нужно?

Каждый фронтенд-разработчик рано или поздно хочет сделать что-то своё — не очередной ToDo-лист, а настоящее **живое** приложение. Мне захотелось бросить вызов самому себе и попробовать силы в роли fullstack-разработчика на реальном проекте. Так родилась идея создать мессенджер — идеальный полигон для изучения real-time технологий, WebRTC и сложной клиентской логики. И да, это отличный способ разобраться, как работают Telegram и WhatsApp под капотом!

В этой статье я расскажу, как создал полноценный веб-мессенджер с:
- **Real-time сообщениями** (никаких Long Polling!)
- **Групповыми чатами** с администрированием
- **Аудио/видео звонками** и демонстрацией экрана
- **PWA**, которое можно установить на телефон

Технологический стек: Vue 3 (Composition API), Node.js, TypeScript, WebSocket, WebRTC, MongoDB. А интерфейс сделал похожим на Telegram — знакомо и удобно.

---

## 🧱 Технологический стек: что, зачем и почему

### **Frontend (моя родная стихия)**
| Технология | Зачем? |
|---|---|
| **Vue 3 + Composition API** | Современный реактивный подход, логика в хуках |
| **TypeScript** | Типы спасают от ошибок в real-time приложениях |
| **Pinia** | Управление состоянием (проще Vuex) |
| **Vite** | Молниеносная сборка и HMR |
| **WebRTC** | P2P-звонки напрямую между браузерами |
| **SCSS + CSS-переменные** | Тёмная/светлая темы из коробки |
| **PWA (vite-plugin-pwa)** | Установка как нативное приложение |

### **Backend (новая территория)**
| Технология | Зачем? |
|---|---|
| **Node.js + Express** | Быстрый старт, знакомый синтаксис |
| **WebSocket (ws)** | Двусторонняя связь в реальном времени |
| **MongoDB + Mongoose** | Гибкая схема для чатов и сообщений |
| **JWT + bcryptjs** | Безопасная аутентификация |
| **Multer** | Загрузка файлов (до 10 МБ) |

### **Инфраструктура**
| Технология | Зачем? |
|---|---|
| **PM2** | Запуск и мониторинг процессов на VPS |
| **Nginx** | Прокси, HTTPS, раздача статики |
| **Let's Encrypt** | Бесплатные SSL-сертификаты |

---

## 🏗️ Архитектура проекта: как всё устроено

```
messenger/
├── backend/                 # Node.js + Express + WebSocket
│   ├── controllers/        # Обработчики REST API
│   ├── middleware/         # Auth, WebSocket-аутентификация
│   ├── models/            # Mongoose-схемы (User, Chat, Message)
│   ├── services/          # WebSocket-сервис (сердце real-time)
│   └── ...
├── frontend/               # Vue 3 приложение
│   ├── composables/       # Переиспользуемая логика (useChat, useWebSocket)
│   ├── stores/            # Pinia (auth, chat, call)
│   ├── components/        # 17 компонентов от ChatWindow до EmojiPicker
│   └── ...
└── ecosystem.config.cjs   # PM2-конфигурация для продакшена
```

---

## 🗄️ Шаг 1: Проектирование базы данных (MongoDB)

Начинал с самого важного — структуры данных. Вот ключевые модели:

### **Пользователь (User)**
```javascript
{
  username: 'nikita',           // Уникальный логин
  status: 'online',             // Онлайн/офлайн/отошёл
  lastSeen: Date,               // Последняя активность
  params: {
    theme: 'dark',              // Тёмная тема по умолчанию
    messageTextSize: 16         // Кастомизация!
  }
}
```

### **Чат (Chat)**
```javascript
{
  type: 'group',                // 'private' или 'group'
  participants: ['id1', 'id2'], // Участники
  groupName: 'Лучшая группа',   // Название (для групповых)
  admin: 'id1',                 // Администратор
  lastMessage: ObjectId         // Для быстрого доступа
}
```

### **Сообщение (Message)**
```javascript
{
  chatId: ObjectId,
  senderId: ObjectId,
  content: 'Привет! Как дела?',
  type: 'text',                 // 'text' | 'image' | 'file' | 'system'
  readBy: ['id1', 'id2'],      // Кто прочитал
  reactions: {                  // Реакции в реальном времени!
    '❤️': ['id1', 'id2'],
    '😂': ['id3']
  }
}
```

---

## 🔐 Шаг 2: Backend — аутентификация и REST API

### **JWT-токены на 7 дней**
- Регистрация/логин возвращают `access_token`
- Каждый запрос проверяется через `auth.middleware`
- WebSocket тоже аутентифицируется через токен в query-параметре

### **Ключевые эндпоинты**
- `POST /api/auth/login` — вход
- `GET /api/chats` — список чатов пользователя
- `POST /api/messages` — отправка сообщения
- `POST /api/upload` — загрузка файлов (изображения, PDF, DOC)

---

## ⚡ Шаг 3: WebSocket — живое общение в реальном времени

WebSocket-сервер поднимается на том же порту, что и HTTP. При подключении клиент отправляет токен — если он невалидный, соединение разрывается.

### **События, которые обрабатываются:**
```javascript
// Сообщения
'socket.emit("message:send", { chatId, content })'
'socket.on("message:new", (message) => { ... })'

// Набор текста (та самая "печатает...")
'socket.emit("typing:start", { chatId })'
'socket.on("typing:update", ({ userId, isTyping }) => { ... })'

// Звонки через WebRTC
'call:start', 'call:accept', 'call:signal'

// Статусы онлайн
'user:status' — рассылка при подключении/отключении
```

**Сложность:** нужно обрабатывать реконнекты, хранить активные соединения и гарантировать доставку событий.

---

## 🎨 Шаг 4: Frontend — Vue 3, Pinia и 17 компонентов

### **Управление состоянием (Pinia)**
- **auth.store** — токен, данные пользователя, вход/выход
- **chat.store** — список чатов, сообщения, непрочитанные
- **call.store** — WebRTC-соединения, медиа-потоки

### **Composables — переиспользуемая логика**
- `useWebSocket()` — подписка на события, управление соединением
- `useChat()` — загрузка сообщений, отправка, пагинация
- `useNotifications()` — toast-уведомления со звуками
- `useConfirm()` — модальные окна подтверждения

### **Ключевые компоненты**
- **ChatWindow.vue** (~3300 строк) — монстр с сообщениями, звонками, реакциями
- **EmojiPicker.vue** — 10 категорий эмоджи + 100+ анимированных ICQ-смайлов
- **MessageInput.vue** — умное поле ввода с превью файлов
- **CallView.vue** — интерфейс звонка с кнопками отключения звука/видео

---

## 📞 Шаг 5: WebRTC — звонки между браузерами

### **Как это работает:**
1. Инициатор вызывает `getUserMedia()` для доступа к камере/микрофону
2. Создаёт `RTCPeerConnection` и генерирует **offer**
3. Отправляет offer через WebSocket получателю
4. Получатель создаёт **answer** и возвращает его
5. Происходит обмен ICE-кандидатами для установления P2P-соединения
6. Медиа-поток передаётся напрямую между браузерами

### **Что реализовано:**
- **Приватные звонки** (1 на 1)
- **Групповые звонки** (mesh-сеть — каждый соединяется с каждым)
- **Демонстрация экрана** через `getDisplayMedia()`
- **Переключение камеры/микрофона** на лету
- **STUN-сервер Google** (`stun:stun.l.google.com:19302`) для проброса NAT

**Проблема:** в корпоративных сетях с симметричным NAT нужен TURN-сервер (планируется в будущем).

---

## 🎭 Шаг 6: Детали, которые оживляют интерфейс

### **Тёмная/светлая тема**
CSS-переменные в `:root`, переключение через `document.documentElement.setAttribute()`.

### **Интерактивность:**
- **Реакции на сообщения** (👍 😂 🔥 ❤️) с real-time обновлением
- **Ответы на сообщения** — цитирование с возможностью перехода к оригиналу
- **Закреплённые сообщения** — отображаются в шапке чата
- **Индикатор "печатает..."** — с задержкой 300 мс и debounce
- **Автоопределение ссылок** — превращаются в кликабельные

### **Оптимизация:**
- **Сжатие изображений** перед загрузкой (до 500 КБ, 1600px)
- **Виртуализация списка сообщений** для чатов с историей
- **Кеширование аватаров** и медиафайлов

---

## 🚀 Шаг 7: Деплой на VPS (Ubuntu + Nginx + PM2)

### **Конфигурация Nginx:**
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    # Frontend
    location / {
        proxy_pass http://localhost:3000;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:5001;
    }

    # WebSocket upgrade
    location /ws {
        proxy_pass http://localhost:5001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

### **PM2-процессы:**
- `shomik-backend` — продакшен-сервер на порту 5001
- `shomik-frontend` — Vite preview на порту 3000
- Плюс dev-версии с hot-reload для разработки

---

## ✅ Итоги первой части: что получилось

### **Работающий мессенджер с:**
- Real-time сообщениями через WebSocket
- Аудио/видео звонками через WebRTC
- Групповыми чатами и администрированием
- Тёмной/светлой темой и адаптивным дизайном
- PWA-установкой на iOS/Android

### **Главные уроки:**
1. **WebSocket** — это просто на старте, но сложно в продакшене (реконнекты, аутентификация, масштабирование)
2. **WebRTC** требует понимания ICE, STUN/TURN и обработки edge-кейсов
3. **TypeScript** — не роскошь, а необходимость для больших real-time приложений
4. **Большие компоненты** нужно вовремя разбивать на composables и дочерние компоненты

### **Что планирую улучшить:**
1. **Docker-контейнеризация** для упрощения деплоя
2. **TURN-сервер** для работы звонков за симметричным NAT
3. **Unit+E2E тесты** (Vitest + Cypress)
4. **Оптимизация WebRTC** для групповых звонков (SFU вместо mesh)

---

**Во второй части** расскажу о проблемах масштабирования, нагрузочном тестировании и о том, как добавлял end-to-end шифрование сообщений. Если есть вопросы по реализации — спрашивайте в комментариях!

---

### **Ключевые улучшения в этой версии:**
1. **Более живой заголовок и введение** — сразу ясно, о чём статья.
2. **Структура с иконками и подзаголовками** — визуально легче читать.
3. **Конкретные примеры кода** в нужных местах.
4. **Выделены сложности и решения** (не только "что сделал", но и "как справился").
5. **Итоги и планы** — показывает прогресс и будущие цели.
6. **Более профессиональный тон**, но с сохранением энтузиазма.

Статья теперь не просто отчёт, а история с вызовами, решениями и полезными insights для других разработчиков.
