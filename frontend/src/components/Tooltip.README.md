# Компонент Tooltip

Универсальный компонент подсказки в стиле приложения Shomik Messenger.

## Использование

### Базовый пример

```vue
<template>
  <Tooltip text="Это подсказка">
    <button>Наведите на меня</button>
  </Tooltip>
</template>

<script setup>
import Tooltip from '@/components/Tooltip.vue';
</script>
```

### Позиционирование

Компонент поддерживает 4 позиции: `top`, `bottom`, `left`, `right` (по умолчанию `top`).

```vue
<Tooltip text="Сверху" position="top">
  <button>Top</button>
</Tooltip>

<Tooltip text="Снизу" position="bottom">
  <button>Bottom</button>
</Tooltip>

<Tooltip text="Слева" position="left">
  <button>Left</button>
</Tooltip>

<Tooltip text="Справа" position="right">
  <button>Right</button>
</Tooltip>
```

### Задержка появления

Можно настроить задержку перед показом подсказки (в миллисекундах):

```vue
<Tooltip text="Появится через 500мс" :delay="500">
  <button>С задержкой</button>
</Tooltip>
```

### Отключение

```vue
<Tooltip text="Не показывается" :disabled="true">
  <button>Отключен</button>
</Tooltip>
```

### С любым контентом

Tooltip оборачивает любой контент через слот:

```vue
<Tooltip text="Информация об иконке">
  <span class="icon">ℹ️</span>
</Tooltip>

<Tooltip text="Подсказка для ссылки">
  <a href="/profile">Профиль</a>
</Tooltip>
```

## Props

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `text` | `string` | `''` | Текст подсказки |
| `position` | `'top' \| 'bottom' \| 'left' \| 'right'` | `'top'` | Позиция подсказки относительно элемента |
| `delay` | `number` | `200` | Задержка перед показом (мс) |
| `disabled` | `boolean` | `false` | Отключить показ подсказки |

## Особенности

- ✅ Автоматическое позиционирование с учетом границ экрана
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Поддержка темной и светлой темы
- ✅ Плавные анимации появления/исчезновения
- ✅ Работает с hover и focus (для доступности)
- ✅ Использует Teleport для правильного позиционирования
- ✅ Автоматическая коррекция позиции при скролле и изменении размера окна

## Примеры использования в проекте

### Замена стандартного title атрибута

**Было:**
```vue
<button :title="callButtonTitle('Голосовой звонок')">
  📞
</button>
```

**Стало:**
```vue
<Tooltip :text="callButtonTitle('Голосовой звонок')">
  <button>📞</button>
</Tooltip>
```

### С иконками действий

```vue
<Tooltip text="Удалить сообщение" position="top">
  <button class="message-action-button">
    <svg>...</svg>
  </button>
</Tooltip>
```

### С информационными иконками

```vue
<Tooltip text="Это поле обязательно для заполнения" position="right">
  <span class="info-icon">ℹ️</span>
</Tooltip>
```

## Стилизация

Компонент использует CSS переменные из темы приложения:
- `--bg-secondary` - фон подсказки
- `--text-primary` - цвет текста
- `--border-color` - цвет границ (если нужно)

Все стили находятся в компоненте и автоматически адаптируются под текущую тему.
