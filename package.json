{
  "name": "shomik-messenger",
  "version": "1.0.0",
  "private": true,
  "description": "Shomik Messenger - Backend and Frontend",
  "scripts": {
    "build": "cd backend && yarn build && cd ../frontend && yarn build && cd ..",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "lint": "yarn --cwd backend lint && yarn --cwd frontend lint",
    "lint:check": "yarn --cwd backend lint:check && yarn --cwd frontend lint:check",
    "test": "yarn --cwd backend test"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "eslint": "^9.39.2"
  }
}
