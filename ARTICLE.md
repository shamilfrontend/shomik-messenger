# Как я пишу мессенджер Часть 1, Что уже получилось

## Зачем я все это делаю?
Все просто, энтузиазм, желание научиться чему то новому на реальном интересном проекте, попробовать себя в роли FullStack-разработчика :-). 

## Введение
В этой статье я расскажу, как я создал полноценный (наверное) Веб-мессенджер с 
real-time сообщениями,
групповыми чатами
аудио/видео звонками с демонстрацией экрана.
Проект написан на Vue 3, NodeJS, WebSocket, TypeScript, 
Разработал похожую на Telegram UI-систему компонентов.

---

## Технологический стек

### Frontend (начнем с него, я ж все таки Фронтендер)

| Технология | Назначение |
|---|---|
| **Vue 3 (Composition API)** | UI-фреймворк |
| **TypeScript** | Типизация |
| **Pinia** | Управление состоянием |
| **Vue Router** | Маршрутизация с навигационными гардами |
| **Axios** | HTTP-клиент |
| **Vite** | Сборка и dev-сервер |
| **SCSS + CSS Variables** | Стилизация с поддержкой тем |
| **WebRTC** | Аудио/видеозвонки |
| **PWA (vite-plugin-pwa)** | Прогрессивное веб-приложение |

### Backend

| Технология | Назначение |
|---|---|
| **Node.js + Express** | HTTP-сервер и REST API |
| **TypeScript** | Типизация |
| **MongoDB + Mongoose** | База данных и ODM |
| **WebSocket (ws)** | Real-time коммуникация |
| **JWT (jsonwebtoken)** | Аутентификация |
| **bcryptjs** | Хеширование паролей |
| **Multer** | Загрузка файлов |
| **PM2** | Управление процессами на сервере |

### Инфраструктура

| Технология | Назначение |
|---|---|
| **PM2** | Управление процессами |
| **MongoDB** | База данных |

---

## Архитектура проекта

```
messenger/
├── backend/
│   └── src/
│       ├── controllers/      # Обработчики запросов
│       ├── middleware/       # Auth, WebSocket middleware
│       ├── models/           # Mongoose-модели
│       ├── routes/           # Express-маршруты
│       ├── services/         # WebSocket-сервис
│       ├── utils/            # JWT, upload, валидация
│       ├── app.ts            # Express-приложение
│       └── server.ts         # Запуск сервера
├── frontend/
│   └── src/
│       ├── components/       # Vue-компоненты (17 шт.)
│       ├── composables/      # Хуки (useAuth, useChat, ...)
│       ├── services/         # API-клиенты
│       ├── stores/           # Pinia-сторы
│       ├── styles/           # SCSS, темы
│       ├── types/            # TypeScript-типы
│       ├── views/            # Страницы
│       └── router/           # Маршрутизация
└── ecosystem.config.cjs      # PM2-конфигурация
```

---

## Шаг 1. Проектирование базы данных

Начали я с проектирования моделей MongoDB:

### User — пользователь
```javascript
{
  username: String,                      // Уникальный логин
  email: String,                         // Email
  password: String,                      // Хеш пароля (bcrypt)
  avatar: String,                        // Аватар (base64)
  status: 'online' | 'offline' | 'away', // Статус пользователя
  lastSeen: Date,
  params: {                              // Набор пользовательских параметров
    messageTextSize: Number,             // размер текста в чате
    theme: String                        // выбранная тема
  }
}
```

### Chat — чат
```javascript
{
  type: 'private' | 'group', // Тип чата
  participants: [ObjectId],  // Участники
  groupName: String,         // Название группы
  groupAvatar: String,       // Аватар (Base64)
  admin: ObjectId,           // Админ группы
  lastMessage: ObjectId,     // Последнее сообщение
  pinnedMessage: ObjectId    // Закреплённое сообщение
}
```

### Message — сообщение
```javascript
{
  chatId: ObjectId,                           // id чата к которому относится сообщение
  senderId: ObjectId,                         // отправитель (id пользователя)
  content: String,                            // содержимое сообщения
  type: 'text' | 'image' | 'file' | 'system', // тип сообщения в чате
  fileUrl: String,                            // ссылка на файл если тип сообщения file
  replyTo: ObjectId,                          // Ответ на сообщение
  readBy: [ObjectId],                         // Кто прочитал
  reactions: Map<String, [ObjectId]>          // Реакции
}
```

---

## Шаг 2. Backend — REST API и аутентификация

### Аутентификация через JWT

Регистрация и логин возвращают JWT-токен со сроком жизни 7 дней. Токен передаётся в заголовке `Authorization: Bearer <token>`.

### Структура API

- **`/api/auth`** — регистрация, логин, получение текущего пользователя
- **`/api/users`** — поиск пользователей, профиль, контакты, смена пароля
- **`/api/chats`** — CRUD чатов, сообщения, реакции, управление группами
- **`/api/upload`** — загрузка файлов (до 10 МБ: изображения, PDF, DOC, TXT)

### Middleware

- **auth.middleware** — проверка JWT-токена в каждом запросе
- **websocket.middleware** — аутентификация WebSocket-соединений через query-параметр `?token=`

---

## Шаг 3. WebSocket — real-time коммуникация

WebSocket-сервер поднимается на том же HTTP-сервере по пути `/ws`. При подключении клиент передаёт JWT-токен для аутентификации.

### Группы событий

**Сообщения:**
- `message:send` → `message:new` — отправка и доставка
- `message:read` — отметка о прочтении
- `message:edited`, `message:deleted` — редактирование и удаление
- `message:reaction` — реакции на сообщения

**Набор текста:**
- `typing:start` / `typing:stop` → `typing:update`

**Звонки:**
- `call:start`, `call:accept`, `call:reject`, `call:hangup` — управление звонком
- `call:signal` — обмен WebRTC-сигналами (offer, answer, ICE candidates)
- `call:join`, `call:leave` — групповые звонки

**Статусы:**
- `user:status` — онлайн/офлайн статус

---

## Шаг 4. Frontend — Vue 3 + Pinia

### Управление состоянием (Pinia)

Три основных стора:

1. **auth.store** — аутентификация, токен, данные пользователя
2. **chat.store** — список чатов, сообщения, набор текста, непрочитанные
3. **call.store** — WebRTC, медиа-потоки, демонстрация экрана

### Composables (переиспользуемая логика)

- `useAuth` — логика авторизации
- `useChat` — операции с чатами
- `useWebSocket` — обработка WebSocket-событий
- `useNotifications` — toast-уведомления со звуком
- `useConfirm` — модальные подтверждения
- `useSettings` — пользовательские настройки
- `useUnreadTitle` — счётчик непрочитанных в заголовке вкладки

### Компоненты (17 штук)

Основные:
- `ChatList` — список чатов с поиском
- `ChatWindow` — окно чата (~3300 строк) с сообщениями, звонками, реакциями
- `MessageInput` — поле ввода с поддержкой эмодзи и файлов
- `EmojiPicker` — выбор эмодзи + коллекция ICQ-смайлов (100+ GIF)
- `AuthForm` — форма входа/регистрации

Модальные окна:
- `CreateGroupModal` — создание группы
- `GroupSettingsModal` — настройки группы
- `UserInfoModal` — информация о пользователе
- `MessageViewModal` — просмотр полного сообщения
- `ConfirmModal` — подтверждение действий

UI-компоненты:
- `Tooltip` — всплывающие подсказки
- `ContextMenu` — контекстное меню (ПКМ)
- `NotificationToast` — уведомления
- `FileUpload` — загрузка файлов

---

## Шаг 5. Аудио/видеозвонки через WebRTC

### Как это работает

1. **Инициатор** вызывает `getUserMedia()` для получения медиа-потока
2. Создаёт `RTCPeerConnection` и отправляет **offer** через WebSocket
3. **Получатель** принимает offer, создаёт **answer** и отправляет обратно
4. Обмен **ICE candidates** для установления P2P-соединения
5. Медиа-потоки передаются напрямую между браузерами

### Возможности

- Приватные звонки (1 на 1)
- Групповые звонки (несколько участников, mesh-сетка)
- Переключение камеры/микрофона во время звонка
- Демонстрация экрана через `getDisplayMedia()`
- Включение/отключение видео и звука

### STUN-сервер

Используется публичный STUN-сервер Google: `stun:stun.l.google.com:19302`

---

## Шаг 6. Стилизация и темы

### Подход

Используем SCSS с CSS-переменными для поддержки тем:

```scss
[data-theme='dark'] {
  --bg-primary: #1a1a2e;
  --bg-secondary: #16213e;
  --text-primary: #e0e0e0;
  --text-secondary: #a0a0a0;
  --accent-color: #6c63ff;
  --border-color: #2a2a4a;
}

[data-theme='light'] {
  --bg-primary: #ffffff;
  --bg-secondary: #f5f5f5;
  --text-primary: #1a1a1a;
  --text-secondary: #666666;
  --accent-color: #6c63ff;
  --border-color: #e0e0e0;
}
```

### Адаптивность

- Десктоп: полная версия с боковой панелью
- Планшет (≤1024px): адаптированная сетка
- Мобильные (≤768px): полноэкранные чаты, адаптация под клавиатуру

---

## Шаг 7. Фичи, которые делают мессенджер живым

### Эмоджи и ICQ-смайлы
Встроенный пикер с Unicode-эмоджи (10 категорий) и коллекцией анимированных ICQ-смайлов (100+ GIF). Сообщения, состоящие только из эмоджи, отображаются увеличенным шрифтом (48px).

### Реакции на сообщения
Набор реакций (👍 😂 🔥 ❤️ 👎 👀 💯) с real-time обновлением. Одна реакция на пользователя на сообщение.

### Ответы и цитирование
Ответ на конкретное сообщение с отображением цитаты и возможностью прокрутки к оригиналу.

### Закреплённые сообщения
Закрепление сообщения в шапке чата с навигацией к нему.

### Редактирование и удаление
Редактирование своих текстовых сообщений, удаление с обновлением последнего сообщения в чате.

### Индикаторы набора текста
Отображение «печатает...» в реальном времени для каждого чата.

### Уведомления
Toast-уведомления со звуком (отдельные звуки для личных и групповых сообщений), fallback через Web Audio API.

### Автоопределение ссылок
Ссылки в сообщениях автоматически становятся кликабельными и открываются в новом окне.

### Сжатие изображений
Клиентская компрессия перед загрузкой: максимум 500 КБ, 1600px по большей стороне.

---

## Шаг 8. PWA — установка как приложение

Настроен `vite-plugin-pwa` для работы как прогрессивное веб-приложение:

- Автообновление Service Worker
- Кеширование статических ресурсов через Workbox
- Манифест для установки на домашний экран
- Поддержка standalone-режима на iOS и Android

---

## Шаг 9. Деплой на VPS

### Стек деплоя

1. **Nginx** — реверс-прокси с HTTPS (Let's Encrypt)
2. **PM2** — управление Node.js-процессами
3. **MongoDB** — база данных

### PM2-конфигурация

Четыре процесса:
- `shomik-backend` — production-сервер API + WebSocket (порт 5001)
- `shomik-frontend` — Vite preview (порт 3000)
- Dev-версии обоих с hot-reload

### Nginx

- Проксирование `/api` и `/uploads` на бэкенд
- WebSocket upgrade для `/ws`
- Раздача фронтенда
- HTTPS с автообновлением сертификатов

---

## Итоги

### Что получилось

Полноценный мессенджер с функциональностью, близкой к Telegram:

- Real-time обмен сообщениями через WebSocket
- Аудио/видеозвонки и демонстрация экрана через WebRTC
- Групповые чаты с администрированием
- Загрузка файлов и изображений
- Эмоджи, реакции, ответы, редактирование
- Темы оформления и адаптивный дизайн
- PWA для установки на устройства

### Чему научились

1. WebSocket — сложнее, чем кажется: нужно обрабатывать реконнекты, аутентификацию и состояние соединения
2. WebRTC — требует понимания ICE, STUN/TURN и mesh-сетей для групповых звонков
3. Большие компоненты Vue — нужно разбивать на composables и подкомпоненты
4. Типизация — TypeScript спасает от огромного количества багов в real-time приложениях

### Что бы улучшил

1. Упаковка приложений в Docker и Docker compose
